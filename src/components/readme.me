We should split / duplicate this, so the ui for the cms exists within the cms, along with the config for shadcn for those items
We'll have "dupliciates" but that's ok, they're serving different purposes - they're not the same, even if the code happens to be identical.

Then we should probably move the blocks out of the cms folder
